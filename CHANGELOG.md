# 변경 이력

버전은 `custom_components/mirot/manifest.json` 과 git 태그, GitHub 릴리스가
항상 같은 값을 씁니다. HACS는 릴리스 태그를 보고 버전을 판단합니다.

---

## v0.5.0 — 2026-07-29

### 바뀜

- **평소에는 기기를 깨우지 않습니다.** 매 주기 `sync` 를 걸어 기기에 보고를
  지시하던 것을, 서버 캐시만 읽고 **1분에 한 번만** 깨우도록 바꿨습니다.

  실측으로 확인한 사실이 근거입니다. HA 폴링과 미로 앱을 모두 멈춰
  아무도 `sync` 를 걸지 않는 상태에서, 리모컨과 본체 버튼으로 조작한
  **7번의 변화가 전부 캐시에 반영**됐습니다. 빠를 때는 3초 만에요.

  | 기기 | 확인한 변화 |
  | --- | --- |
  | MF03 (선풍기) | `Power`, `FanSpeed`, `RotationRange` — 6회 |
  | NR07 (가습기) | `Power`, `SteamLevel` — 1회 |

  즉 **기기는 조작이 있으면 스스로 상태를 올립니다.** 그래서 조작 감지에는
  `sync` 가 필요 없습니다. 다만 변화가 없으면 아무것도 올리지 않아
  온습도·배터리가 멈추므로, 그 값을 새로 받고 혹시 놓친 변화를 바로잡는
  용도로만 가끔 깁니다.

- 제어 명령 직후의 확인 조회는 그대로 기기를 깨웁니다. 캐시만 읽으면 기기가
  아직 보고하기 전이라 방금 바꾼 값이 안 담겨 있을 수 있고, 그러면 낙관적
  반영이 만료되는 순간 화면이 옛 값으로 되돌아갑니다.

### 효과

- 요청 하나당 서버 처리가 **약 0.8초에서 0.15초로** 줄었습니다
- 기기를 5초마다 깨우지 않습니다. 무선 제품 배터리에 유리합니다
- **반응 속도는 그대로입니다.** 기존에도 `sync` 는 직전 값을 돌려주고 새 값은
  다음 주기에 읽혔기 때문에, 결국 기기가 올린 값을 다음 주기에 읽는 지금과
  같은 타이밍입니다

### 알아둘 점

- 검증은 MF03·NR07 두 기종입니다. 다른 모델이 다르게 동작한다면 1분 주기
  `sync` 가 그물 역할을 하므로 최대 1분 안에 바로잡힙니다

---

## v0.4.1 — 2026-07-29

### 바뀜

- 기기가 전부 꺼져 있을 때의 확인 주기를 **30초 → 20초**로 줄였습니다.
  리모컨으로 켰을 때 HA가 알아채기까지 30초는 길다고 봤습니다.
  요청 절감은 62% → 56% 로 줄지만 체감 지연이 3분의 2가 됩니다.

---

## v0.4.0 — 2026-07-29

### 추가

- **적응형 폴링.** 기기가 전부 꺼져 있으면 주기를 늦추고, 하나라도 켜지면
  설정한 주기(기본 5초)로 돌아옵니다.
  하루 6시간 사용 기준 요청이 17,280 → 약 6,500회로 줄었습니다.

  미로 서버는 푸시를 주지 않고 기기가 스스로 상태를 올리지도 않아 폴링
  말고는 방법이 없습니다. 그래서 주기를 균일하게 늦추는 대신, 값이 바뀔
  일이 없는 구간만 늦추는 쪽을 골랐습니다.

  비교 삼아, 같은 종류인 VeSync(공기청정기·가습기·선풍기)가 60초,
  Sensibo·Roborock 60초, ecobee 180초, Awair 300초입니다.

### 알아둘 점

- 꺼져 있는 기기를 리모컨이나 본체 버튼으로 켜면 HA가 그만큼 늦게 압니다.
  HA에서 켤 때는 즉시 반영되므로 해당 없습니다.
- 설정 주기가 이미 유휴 주기보다 길면 그대로 씁니다. 더 빠르게 만들지 않습니다.
- 전원 상태를 아무도 보고하지 않는 구성이면 켜졌는지 알 수 없으므로
  빠른 쪽으로 둡니다.

---

## v0.3.1 — 2026-07-29

### 추가

- **통합 브랜드 아이콘.** `custom_components/mirot/brand/` 에 넣었습니다.
  파란 집 심볼에 가운데 M, 가로형은 심볼 + `miro` 워드마크(Inter Bold).
  다크 테마용 로고를 따로 넣어 어두운 배경에서 글씨가 사라지지 않습니다.

  **Home Assistant 2026.3 이상**에서만 보입니다. 그 아래에서는 비어 보이지만
  동작에는 영향이 없어서 최소 지원 버전은 2024.11.0 그대로 뒀습니다.

  `home-assistant/brands` 저장소는 커스텀 통합 아이콘을 더 이상 받지 않으며
  PR을 봇이 자동으로 닫습니다. 2026.3 부터는 통합이 직접 제공합니다.

- 미로컨 관련 `unavailable` 진단법을 README에 적었습니다.
  NR07은 온습도 센서가 무선 리모컨(미로컨)에 달려 있어, 온도·습도·배터리가
  한꺼번에 `unavailable` 이면 미로컨을 봐야 합니다. 가습기는 배터리로 돌지
  않으므로 그 배터리 센서는 본체가 아니라 미로컨 것입니다.

---

## v0.3.0 — 2026-07-29

### 바뀜

- **제어가 화면에 즉시 반영됩니다.** 기기는 명령을 받는 즉시 움직이는데
  화면은 최대 5초 뒤에야 따라오던 문제를 고쳤습니다.

  조회는 '직전' 값을 돌려주고 새 값은 1초쯤 뒤에야 서버 캐시에 들어갑니다.
  그래서 명령 직후 곧바로 다시 읽던 기존 동작은 거의 항상 옛 값을 받아
  헛돌았고, 결국 다음 정기 폴링까지 기다려야 했습니다.

  - 명령이 받아들여지면 그 값을 화면에 먼저 반영합니다
  - 확인 조회는 즉시가 아니라 1.5초 뒤에 한 번
  - 그 사이 정기 폴링이 끼어들어 옛 값으로 화면을 되돌리지 않도록 막았습니다

- 기기에서 연동되는 동작도 함께 반영합니다. 풍량을 보내면 전원이 같이 켜지고,
  회전 범위가 0이 아니면 회전이 켜집니다. 이걸 빠뜨리면 꺼진 선풍기에 풍량을
  넣었을 때 전원이 Off 인 채라 0%로 표시됩니다.

### 일부러 안 한 것

- 가습기 전원을 켤 때 분무 세기가 "약"으로 바뀌는 것은 반영하지 않았습니다.
  모델마다 값이 달라 추측이 되므로 1.5초 뒤 확인 조회에 맡깁니다.

---

## v0.2.1 — 2026-07-29

### 고침

- **일시적 조회 실패로 엔티티가 깜빡이던 문제.**
  조회는 기기가 5초 안에 응답하기를 기다리는데, 무선 기기라 한 주기쯤 놓치는
  일이 드물지 않습니다. 그때마다 엔티티 전체가 `사용 안 됨` 으로 떨어졌다가
  다음 주기에 돌아왔습니다. 기기 여러 대를 한 번에 묶어 묻기 때문에 한 대만
  늦어도 전체가 같이 죽었습니다.

  - 실패하면 서버 캐시만 읽어 한 번 더 시도합니다
  - 그마저 실패해도 연속 3회까지는 직전 값을 유지합니다

- **HA 이벤트 루프를 막던 파일 읽기.** 모델 표(`models.json`)를 이벤트 루프
  안에서 읽어 HA가 `blocking call` 경고를 남기고 있었습니다. 셋업 때 별도
  스레드에서 한 번만 읽도록 바꿨습니다.

---

## v0.2.0 — 2026-07-29

첫 공개.

미로 스마트홈 앱(`com.miro.mirotapp`)이 쓰는 miroT v2 클라우드 API로 미로
기기를 HA에 직접 붙입니다. SmartThings 경유 없이 풍량 1~100, 회전각
0/30/60/90/120도를 그대로 씁니다.

- `fan` `humidifier` `switch` `select` `sensor` `binary_sensor`
- 서버 메타데이터에서 뽑은 모델 표(27종)와 기기가 보고하는 기능 목록을
  겹쳐 엔티티를 구성합니다. 모델에 없는 기능은 엔티티도 생기지 않습니다
- 계정 로그인만으로 기기 자동 검색, 토큰 만료 시 자동 갱신
- `tools/miro_cli.py` — HA 없이 API를 확인하는 CLI. 이슈에 붙일 기기 응답을
  이걸로 뽑습니다
- `tools/gen_models.py` — 서버 메타데이터에서 모델 표 생성

실기기 검증은 **MF03**(선풍기), **NR07**(가습기) 두 종입니다. 나머지 25종은
서버 정의만 확인했습니다. 값 체계를 모르는 항목은 추측해서 명령을 보내지 않고
설치 시 로그에 건너뛴 내용을 남깁니다.

미로는 `미로 스마트홈` 과 `미로 홈링크` 두 앱을 따로 운영하며 서버가 다릅니다.
홈링크 쪽은 Tuya 기반이라 이 통합의 대상이 아닙니다.
